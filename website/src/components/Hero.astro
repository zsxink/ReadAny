---
interface Props {
  t: (key: string) => string
}

const { t } = Astro.props
---

<!-- Vercel-style: Clean, minimal, lots of whitespace -->
<section class="py-32">
  <div class="mx-auto max-w-5xl px-6">
    <div class="max-w-3xl">
      <h1 class="text-4xl font-semibold tracking-tight sm:text-5xl">
        {t('hero.title')}
      </h1>
      
      <p class="mt-6 text-lg text-muted-foreground leading-relaxed max-w-2xl">
        {t('hero.subtitle')}
      </p>

      <div class="mt-10 flex flex-wrap gap-3">
        <a 
          href="#download" 
          class="inline-flex items-center justify-center rounded-full bg-foreground px-6 py-2.5 text-sm font-medium text-background transition-colors hover:bg-foreground/80"
        >
          {t('hero.download')}
        </a>
        <a 
          href="#features" 
          class="inline-flex items-center justify-center rounded-full border border-border px-6 py-2.5 text-sm font-medium transition-colors hover:border-foreground/30 hover:bg-muted/50"
        >
          {t('hero.learn')}
        </a>
      </div>

      <p class="mt-6 text-sm text-muted-foreground">
        {t('hero.platforms')}
      </p>
    </div>

    <!-- Screenshot -->
    <div class="mt-20 rounded-xl border border-border/50 overflow-hidden">
      <div class="aspect-[16/9] flex items-center justify-center bg-muted/30">
        <div class="text-center">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="mx-auto h-10 w-10 text-muted-foreground/30">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
          </svg>
          <p class="mt-3 text-sm text-muted-foreground">{t('hero.screenshot')}</p>
        </div>
      </div>
    </div>

    <!-- Stats: 移动端2列，桌面端4列 -->
    <div class="mt-20 grid grid-cols-2 sm:grid-cols-4 gap-8">
      <div class="text-center">
        <div class="text-2xl font-semibold">8+</div>
        <div class="mt-1 text-sm text-muted-foreground">{t('hero.stats.formats')}</div>
      </div>
      <div class="text-center">
        <div class="text-2xl font-semibold">5+</div>
        <div class="mt-1 text-sm text-muted-foreground">{t('hero.stats.providers')}</div>
      </div>
      <div class="text-center">
        <div class="text-2xl font-semibold">100%</div>
        <div class="mt-1 text-sm text-muted-foreground">{t('hero.stats.local')}</div>
      </div>
      <div class="text-center">
        <div class="text-2xl font-semibold">GPL-3.0</div>
        <div class="mt-1 text-sm text-muted-foreground">{t('hero.stats.opensource')}</div>
      </div>
    </div>
  </div>
</section>
